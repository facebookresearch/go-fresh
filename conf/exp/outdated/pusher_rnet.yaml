# @package _global_
defaults:
    - /env/pusher

rnet:
    dataset:
        symmetric: False
    model:
        remove_velocity: False
    memory:
        directed: True
        capacity: 1000

main:
        load_from_dir: /checkpoint/linamezghani/offline-gcrl/rnet/pusher_thresh5
