# @package _global_
defaults:
    - /config

env:
    id: quadruped
    obs_type: vec
    success_thresh: 0.7
    max_episode_steps: 1000
    action_repeat: 2
    obs:
        type: vec
        state_size: 57
        vec_size: 78
        rgb_size: 64
    action_dim: 12

exploration_buffer:
    data_dir: /checkpoint/${env:USER}/offline-gcrl/data/quadruped/proto/buffer

rnet:
    model:
        remove_velocity: false
        dims_to_keep:
